Follow the these steps:
1. Open the Command Palette (`Ctrl+Shift+P`), type Python: Select Interpreter, and choose the `.venv` path.
2. Run `pip install "fastapi[standard]"` in the terminal.
3. Run `fastapi dev main.py` in the terminal.