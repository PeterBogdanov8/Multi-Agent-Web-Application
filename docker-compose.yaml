services:
  backend:
    build: /backend
    command: fastapi dev main.py --host 0.0.0.0
    ports:
      - 8000:8000
  
  frontend:
    build: /frontend
    command: npm run start -- --host 0.0.0.0
    ports:
      - 4200:4200
    depends_on:
      - backend